<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="org.omri">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- AndroidUsingLinkProperties.setup(Context) requires ACCESS_NETWORK_STATE -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>
